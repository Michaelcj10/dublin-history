{
  "name": "dublin-timeline",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",

    "generate": "npx tsx scripts/generate.ts",
    "generate:images": "npx tsx scripts/generate-images.ts",
    "generate:all": "npm run generate && npm run generate:images && npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.3",
    "dotenv": "^16.4.5",
    "next": "14.1.0",
    "openai": "^4.47.1",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "tsx": "^4.7.0",
    "typescript": "^5"
  }
}